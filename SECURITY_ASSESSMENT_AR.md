# تقرير المراجعة الأمنية المختصر — IDEA Business

**التاريخ:** 2026-07-14  
**المرجع:** OWASP Top 10 (2021)  
**نوع الفحص:** مراجعة كود ثابتة + فحص تبعيات + اختبار بناء

## الملخص التنفيذي

تمت مراجعة المشروع وتطبيق تشديدات أمنية مباشرة مع الحفاظ على منطق التطبيق. المشروع كان يحتوي أصلًا على عناصر حماية جيدة، منها مصادقة Supabase، وفحص صلاحيات الإدارة، وسياسات RLS، والتحقق من توقيعات Stripe وFatora، وWAF، وترويسات أمنية، وتسجيل أحداث أمنية.

لا يمكن إثبات أن أي تطبيق «خالٍ 100% من الثغرات» من خلال مراجعة كود ثابتة فقط؛ يلزم أيضًا فحص ديناميكي على بيئة تشغيل، واختبارات صلاحيات قاعدة البيانات، واختبار اختراق مصرح به. النسخة الحالية أكثر أمانًا وتم إصلاح المشكلات المؤكدة التي تم تحديدها.

## الثغرات والمشكلات التي كانت موجودة

- **CORS واسع على واجهات الإدارة**: كانت استجابات `/api/admin/*` تسمح بـ `Access-Control-Allow-Origin: *`، وهو إعداد غير مناسب للواجهات ذات الصلاحيات العالية.
- **ضعف التحقق من المدخلات الإدارية**: بعض وظائف إدارة الحماية كانت تمرر قيم IP وUUID والحدود المالية والمدد والنصوص دون مخطط تحقق صارم.
- **غياب تحقق منطقي للحدود المالية**: كان من الممكن إرسال حد لكل عملية أكبر من الحد اليومي.
- **حدود طلب غير كافية في endpoint عام للتسجيل**: مسار client telemetry لم يكن يفرض نوع المحتوى أو حدًا معلنًا لحجم جسم الطلب.
- **مشكلة إدارة تبعيات**: `npm ci` العادي يفشل بسبب تعارض peer dependency بين `@tanstack/zod-adapter` وZod 4؛ تم التحقق باستخدام وضع legacy peer dependencies دون تغيير معماري مخاطِر.
- **أخطاء TypeScript قديمة غير أمنية**: توجد روابط لمسارات غير معرفة مثل `/admin/ai-assistant` و`/project/$id`. هذه ليست ناتجة عن الإصلاحات الأمنية لكنها تحتاج معالجة وظيفية مستقلة.

## الإصلاحات المنفذة

- إزالة wildcard CORS من واجهات الإدارة وجعل استخدامها من نفس الأصل فقط.
- إضافة `Cache-Control: no-store` و`Vary: Origin` وحد معقول لذاكرة preflight.
- إضافة تحقق Zod صارم لعناوين IPv4 وIPv6، ومعرفات UUID، ومستويات الخطورة، والأسباب، والمدد، والحدود المالية.
- تقييد القيم المالية إلى أعداد صحيحة غير سالبة ضمن حد أقصى معقول.
- منع الحد لكل عملية من تجاوز الحد اليومي.
- فرض `application/json` على endpoint التسجيل العام.
- رفض الطلبات التي يتجاوز `Content-Length` المعلن فيها 16 KiB.
- تحديث سجل الإصلاحات الأمنية داخل `SECURITY_FIXES.md`.
- تنفيذ فحص تبعيات إنتاجية؛ النتيجة: **0 ثغرات معروفة** وقت الفحص.

## تصنيف OWASP Top 10

| التصنيف | المشكلة | الخطورة قبل الإصلاح | الحالة |
|---|---|---:|---|
| A01: Broken Access Control | CORS غير مناسب لمسارات الإدارة | متوسطة | تم الإصلاح |
| A04: Insecure Design | عدم اتساق الحدود المالية | متوسطة | تم الإصلاح |
| A05: Security Misconfiguration | wildcard CORS وعدم منع التخزين المؤقت | متوسطة | تم الإصلاح |
| A03: Injection | مدخلات إدارية غير مقيدة بما يكفي | متوسطة | تم التشديد |
| A09: Security Logging and Monitoring Failures | احتمال إساءة استخدام endpoint التسجيل | منخفضة–متوسطة | تم التشديد |
| A06: Vulnerable and Outdated Components | فحص الحزم الإنتاجية | منخفضة | 0 ثغرات معروفة وقت الفحص |

## نتائج الاختبار

- تثبيت التبعيات نجح باستخدام `--legacy-peer-deps`.
- `npm audit --omit=dev`: لم يُظهر ثغرات معروفة وقت الفحص.
- تم إنشاء مجلد مخرجات الإنتاج `.output` أثناء اختبار البناء.
- فحص TypeScript أظهر أخطاء روابط قديمة في ملفات صفحات أخرى؛ لم تظهر أخطاء في الملفات الأمنية المعدلة بعد تصحيح مخطط IP.

## توصيات قبل الإطلاق النهائي

- تشغيل اختبارات تكامل فعلية على Supabase والتأكد من RLS لكل جدول ودالة RPC.
- اختبار صلاحيات الأدوار: user، moderator، accountant، admin.
- تشغيل DAST مصرح به باستخدام OWASP ZAP على بيئة staging.
- إصلاح روابط المسارات القديمة وإضافةها لاختبارات CI.
- تثبيت إصدارات الحزم وحل تعارض Zod/TanStack بدل الاعتماد الدائم على legacy peer dependencies.
- تدوير أي أسرار حقيقية إن سبق وضعها في المستودع أو السجل التاريخي لـ Git.

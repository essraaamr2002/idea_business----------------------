# تقرير إعادة الفحص الأمني الاحترافي

**المشروع:** IDEA BUSINESS  
**التاريخ:** 14 يوليو 2026  
**المنهجية:** مراجعة كود ثابتة موجهة حسب OWASP Top 10، مراجعة المصادقة والصلاحيات، Webhooks، CORS/CSP، التحقق من المدخلات، الأسرار، رفع الملفات، والتبعيات.

## الملخص التنفيذي

تمت إعادة مراجعة النسخة المُصلحة واكتشاف نقاط إضافية لم تكن معالجة بالكامل في المراجعة السابقة. تم تطبيق إصلاحات دفاعية مع الحفاظ على بنية المشروع. أظهر `npm audit --omit=dev` عدم وجود ثغرات معروفة في حزم الإنتاج وقت الفحص.

لا يمكن لأي مراجعة كود ثابتة وحدها إثبات أن التطبيق خالٍ 100% من الثغرات؛ يلزم بعد النشر اختبار ديناميكي وتهيئة صحيحة لـSupabase RLS والأسرار والبنية السحابية.

## الثغرات والمشكلات المكتشفة

- ترويسة CORS مفتوحة بقيت في مسار تصدير بيانات الإدارة.
- إمكانية CSV Formula Injection داخل ملفات التصدير عند فتحها ببرامج الجداول.
- عدم وجود فحص موحد لمصدر الطلبات الحساسة الخاصة بالإدارة.
- عدم تقييد نوع وحجم جسم طلب تصدير الإدارة.
- سياسة CSP كانت تسمح بـ`unsafe-eval`.
- نقطة تسجيل أخطاء العميل كانت تسمح بطلبات CORS من أي موقع.
- Webhooks تقرأ جسم الطلب كاملًا دون حد أقصى واضح.
- مقارنة بعض أسرار مهام Cron كانت مقارنة نصية عادية.
- وجود قيمة Salt احتياطية ثابتة لمسار الإحالة عند غياب متغير البيئة.
- استخدام `Math.random()` في أسماء ملفات مرفوعة.

## الإصلاحات المنفذة

- إزالة wildcard CORS من تصدير الإدارة وإضافة `no-store` و`nosniff`.
- حماية CSV من القيم التي تبدأ بـ `=`, `+`, `-`, `@`, tab أو carriage return.
- إضافة فحص Same-Origin للطلبات الإدارية المعدّلة للحالة مع استمرار دعم عملاء API المصرح لهم عبر Bearer token.
- إضافة مخطط Zod صارم لطلبات التصدير وتقييد النوع والحجم.
- إزالة `unsafe-eval` من CSP.
- جعل CORS لمسار client telemetry محصورًا بالمصدر نفسه ورفض المصادر الخارجية.
- إضافة قارئ طلبات محدود الحجم للـStripe وFatora Webhooks بحد 1 MiB.
- استخدام `timingSafeEqual` لمقارنة أسرار Cron.
- إلغاء Salt الافتراضي الضعيف وفرض `REFERRAL_HASH_SALT` بطول مناسب.
- استبدال `Math.random()` بـ`crypto.randomUUID()` في أسماء الملفات المرفوعة.
- إضافة وحدة مشتركة `http-security.server.ts` للاستجابات الآمنة، فحص JSON، الحجم، Same-Origin، والمقارنة الزمنية الثابتة.

## تصنيف OWASP Top 10

- **A01: Broken Access Control** — CORS الإداري وفحص مصدر الطلبات الحساسة.
- **A02: Cryptographic Failures** — Salt افتراضي ثابت ومقارنات أسرار غير ثابتة زمنيًا.
- **A03: Injection** — CSV Formula Injection والتحقق غير الصارم من جسم طلب التصدير.
- **A04: Insecure Design** — غياب حدود مركزية لأحجام الطلبات.
- **A05: Security Misconfiguration** — `unsafe-eval` وCORS المفتوح.
- **A06: Vulnerable and Outdated Components** — تمت مراجعة التبعيات؛ 0 ثغرات معروفة وقت الفحص، مع وجود حزم مهجورة وتحذيرات ترحيل ينبغي تحديثها مستقبلًا.
- **A08: Software and Data Integrity Failures** — تقوية أسماء الملفات ومنع صياغات CSV التنفيذية.
- **A10: Server-Side Request Forgery** — لم يتم تأكيد SSRF مباشر ضمن المسارات التي تمت مراجعتها، لكن يجب الاستمرار في تقييد أي URL خارجي بخوادم التكامل.

## التحقق

- `npm ci --legacy-peer-deps --ignore-scripts`: نجح.
- `npm audit --omit=dev`: **0 vulnerabilities**.
- Prettier للملفات المعدلة: نجح.
- بدأ بناء Vite وتحويل ملفات العميل والخادم دون ظهور خطأ متعلق بالإصلاحات، لكنه تجاوز مهلة بيئة الفحص بسبب حجم المشروع وكثرة التحذيرات القديمة من TanStack.
- فحص TypeScript الكامل لم يعطِ نتيجة نهائية ضمن المهلة المتاحة.

## ملاحظات تشغيلية إلزامية قبل الإنتاج

- ضبط أسرار قوية وفريدة: `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`, `STRIPE_WEBHOOK_SECRET`, `FATORA_WEBHOOK_SECRET`, `REFERRAL_HASH_SALT`.
- عدم وضع أي Secret يبدأ بـ`VITE_` أو داخل مستودع Git.
- اختبار Supabase RLS لكل جدول باستخدام حساب مستخدم عادي وحساب Admin.
- تشغيل OWASP ZAP على بيئة Staging، واختبار IDOR، CSRF، رفع الملفات، Webhooks، واسترجاع كلمة المرور.
- نقل Rate Limiting إلى Redis/Cloudflare Durable Objects أو بوابة API موزعة بدل الاعتماد على الذاكرة المحلية فقط.
- ترقية TanStack APIs المهجورة وحزم React Email/Recharts المهجورة تدريجيًا بعد اختبار الانحدار.
